怎么说呢？我比较喜欢经典的界面。（以前小学用 XP 的时候喜欢把主题设为“Windows 经典样式”。）因此这个网站我也以自己理想中的样子去做了这个站点。
但是很遗憾，一些功能在 Internet Explorer 7.0 和更低版本中没有顺利实现。有 Bug。
另外，将来（有机会的话）我打算将站点做成动态网页，服务器能自动生成模板而不再需要我为每个网页生成模板，同时，文章列表也可能会设计成打开时服务器检索并返回，且搜索也会利用同一方法，从提交命令到服务器并返回结果，然后还要支持无限滚动。且为旧浏览器支持分页显示模式。同时也会像 Google 搜索一样为旧版本浏览器专门设计一个样式。